import * as Constants from './constants';

const initialState = {
  unreadMessagesCount: 11,
};

export default (state = initialState, action) => {
  switch(action.type) {

    default:
      return state;
  }
}
